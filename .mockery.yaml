with-expecter: True
inpackage: False
mockname: "Mock{{.InterfaceName}}"
outpkg: "mocks"
filename: "mock_{{.InterfaceName}}.go"
dir: "{{.InterfaceDir}}/mocks"
all: True
recursive: True
packages:
  sum/pkg/command:
  sum/pkg/adapter:
  sum/pkg/listener:
  sum/pkg/logger: 
